const removeCard = require('../../../../../server/reducers/addCard/utils/removeCard');
const gameState = require('../../../../../__fixtures__/testGameState');

describe('removeCard', () => {
    it('should remove card from player', () => {
        const player = gameState.data.players[0];
        
    });
});
